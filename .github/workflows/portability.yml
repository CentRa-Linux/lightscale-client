name: portability

on:
  push:
    branches:
      - main
      - master
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  linux-platform:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Linux deps
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends pkg-config libmnl-dev libnftnl-dev

      - name: Cargo check (linux)
        run: cargo check

      - name: Platform guard tests
        run: cargo test platform::tests

      - name: Platform command smoke
        run: |
          cargo run -- platform --json > /tmp/platform-linux.json
          grep -q '"os": "linux"' /tmp/platform-linux.json

  macos-platform:
    runs-on: macos-14
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cargo check portability layer (macOS)
        run: cargo check --lib

      - name: Platform guard tests (macOS)
        run: cargo test --lib platform::tests

  windows-platform:
    runs-on: windows-2022
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cargo check portability layer (Windows)
        run: cargo check --lib

      - name: Platform guard tests (Windows)
        run: cargo test --lib platform::tests
